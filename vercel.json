{
  "version": 2,
  "buildCommand": "npx expo export --platform web && exit 0",
  "outputDirectory": "dist",
  "cleanUrls": true,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
